spring.application.name=payment-service
server.port=8083

# ── Order Service (inter-service REST call) ───────────────────────────
order.service.url=${ORDER_SERVICE_URL:http://localhost:8082}

management.endpoints.web.exposure.include=health,info
